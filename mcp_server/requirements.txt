# DSMS MCP Server 依赖
#
# 注意：本文件需在独立虚拟环境中安装，不可与主项目共用 venv。
# mcp[cli] 依赖 starlette>=0.52，与主项目 fastapi==0.115.6 的 starlette<0.42 冲突。
#
# 创建独立环境：
#   python -m venv mcp_server/.venv
#   mcp_server\.venv\Scripts\activate   # Windows
#   pip install -r mcp_server/requirements.txt

mcp[cli]>=1.6.0
httpx>=0.27.0
